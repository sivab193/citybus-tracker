# CityBus Bot — Production Requirements

# ─── Core ───
python-telegram-bot[job-queue]>=20.0
fastapi
uvicorn[standard]
pydantic
python-dotenv

# ─── Data ───
gtfs-realtime-bindings
rapidfuzz

# ─── Database ───
motor                           # MongoDB async driver
pymongo                         # MongoDB sync (for scripts)
dnspython                       # Required for mongodb+srv connection strings
certifi                         # Root certificates for SSL
redis[hiredis]                  # Redis async with C parser

# ─── HTTP ───
requests

# ─── MCP ───
mcp[cli]


# ─── Testing & Development (pip install -r requirements.txt) ───
pytest
httpx
pytest-asyncio
discord.py>=2.3.0
aiohttp
